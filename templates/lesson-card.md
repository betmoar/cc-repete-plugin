<!--
  One lesson per file in .repete/lessons/. Name files NNN-short-slug.md (e.g. 003-jest-esm-mock.md).
  Keep each card tight — this is a retrieval store, not a journal. If a card duplicates an
  existing one, update the existing card and bump its `hits` instead of adding a new file.
-->
---
slug: <short-kebab-slug>
tags: [<area>, <tool>, <symptom>]   # used to decide which lessons to surface into a loop
severity: low | medium | high       # how badly it bit
hits: 1                              # increment each time this recurs
created: <YYYY-MM-DD>
---

**Situation:** <when this comes up — the trigger conditions>

**Tried:** <what was attempted>

**Outcome:** <what actually happened — the failure or the win>

**Rule:** <the imperative takeaway for next time — what to do or avoid>
